<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../views/partials/head'); -%>
</head>
<body>
    <main>

        
       <% items.forEach(function(item){ %>
        <a href="/campgrounds/<%= item._id %>">
        <div>
            <h1><%= item.title %></h1>
            <p>Hello human!</p>
        </div>
        </a>

        <% }); %>
       


        </div>
    </main>
</body>
</html>